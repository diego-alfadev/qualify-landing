<div class="grid grid-cols-1 md:grid-cols-12 gap-6 mb-24">
  <!-- Main Stats Card -->
  <div
    class="md:col-span-7 glass-panel rounded-3xl p-8 relative overflow-hidden"
  >
    <div
      class="absolute top-0 right-0 w-64 h-64 bg-emerald-500/5 blur-[80px] rounded-full pointer-events-none"
    >
    </div>

    <div class="flex justify-between items-start mb-8">
      <div>
        <p
          class="text-xs font-medium tracking-widest text-emerald-500 mb-2 uppercase"
        >
          Canales de Entrada
        </p>
        <h3 class="text-3xl font-medium text-white tracking-tight">
          Embudo Total
        </h3>
      </div>
      <span
        class="px-3 py-1 rounded-full bg-emerald-500/10 border border-emerald-500/20 text-emerald-400 text-xs font-medium flex items-center gap-1"
      >
        <i data-lucide="trending-up" class="w-3 h-3"></i> +24% Eficiencia
      </span>
    </div>

    <div class="grid grid-cols-3 gap-6 mt-12">
      <!-- Stat 1 -->
      <div class="space-y-2">
        <div class="flex items-center gap-2 text-gray-400 text-xs uppercase">
          <i data-lucide="message-circle" class="w-3 h-3"></i> WhatsApp IA
        </div>
        <p class="text-2xl font-medium text-white">842</p>
        <p class="text-xs text-emerald-500 font-mono">+5.8% hoy</p>
      </div>
      <!-- Stat 2 -->
      <div class="space-y-2">
        <div class="flex items-center gap-2 text-gray-400 text-xs uppercase">
          <i data-lucide="phone-missed" class="w-3 h-3"></i> Calls Rec.
        </div>
        <p class="text-2xl font-medium text-white">128</p>
        <p class="text-xs text-emerald-500 font-mono">+12.4% hoy</p>
      </div>
      <!-- Stat 3 -->
      <div class="space-y-2">
        <div class="flex items-center gap-2 text-gray-400 text-xs uppercase">
          <i data-lucide="globe" class="w-3 h-3"></i> Web Form
        </div>
        <p class="text-2xl font-medium text-white">415</p>
        <p class="text-xs text-red-400 font-mono">-2.1% hoy</p>
      </div>
    </div>

    <!-- Graph Line Simulation -->
    <div class="mt-8 h-1 w-full bg-gray-800 rounded overflow-hidden">
      <div
        class="h-full w-2/3 bg-gradient-to-r from-emerald-600 to-emerald-400"
      >
      </div>
    </div>
  </div>

  <!-- Secondary Feature Card (Smart Path) -->
  <div class="md:col-span-5 glass-panel rounded-3xl p-8 relative">
    <div class="flex justify-between items-center mb-8">
      <span class="text-xs font-medium tracking-widest text-gray-400 uppercase"
        >Flujo Inteligente</span
      >
      <div
        class="px-3 py-1 rounded-full border border-white/10 text-[10px] text-gray-400"
      >
        Ruta optimizada
      </div>
    </div>

    <!-- Flow Visualization -->
    <div class="relative py-4">
      <!-- Connection Line -->
      <div
        class="absolute left-6 top-0 bottom-0 w-px bg-gradient-to-b from-transparent via-emerald-500/50 to-transparent border-l border-dashed border-emerald-500/30"
      >
      </div>

      <!-- Step 1 -->
      <div class="relative flex items-center gap-4 mb-6">
        <div
          class="w-12 h-12 rounded-full bg-black border border-white/10 flex items-center justify-center z-10"
        >
          <span class="text-xs font-mono text-gray-400">IN</span>
        </div>
        <div>
          <p class="text-sm text-white font-medium">Recepción Lead</p>
          <p class="text-xs text-gray-500">Portal / WhatsApp / Tel</p>
        </div>
      </div>

      <!-- Step 2 (Active) -->
      <div class="relative flex items-center gap-4 mb-6">
        <div
          class="w-12 h-12 rounded-full bg-emerald-900/30 border border-emerald-500/50 flex items-center justify-center shadow-[0_0_15px_rgba(16,185,129,0.3)] z-10"
        >
          <i data-lucide="cpu" class="w-5 h-5 text-emerald-400"></i>
        </div>
        <div
          class="bg-emerald-900/10 border border-emerald-500/20 rounded-lg p-3 flex-1"
        >
          <div class="flex justify-between items-center">
            <p class="text-sm text-white font-medium">Qualify Engine</p>
            <span class="text-[10px] text-emerald-400 font-mono">0.09s</span>
          </div>
          <p class="text-xs text-gray-400 mt-1">
            Filtrado & Scoring automático
          </p>
        </div>
      </div>

      <!-- Step 3 -->
      <div class="relative flex items-center gap-4">
        <div
          class="w-12 h-12 rounded-full bg-black border border-white/10 flex items-center justify-center z-10"
        >
          <span class="text-xs font-mono text-gray-400">CRM</span>
        </div>
        <div>
          <p class="text-sm text-white font-medium">Agencia Notificada</p>
          <p class="text-xs text-gray-500">Lead cualificado entregado</p>
        </div>
      </div>
    </div>
  </div>
</div>
